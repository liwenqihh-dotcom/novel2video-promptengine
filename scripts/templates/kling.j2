[positive_prompt]
起始：{{ start_frame }}。过渡：{{ peak_frame }}。结束：{{ end_frame }}。保持角色一致：{{ lock_summary }}。场景为{{ time }}的{{ location }}，{{ atmosphere }}氛围，{{ lighting }}。镜头从{{ shot_type }}开始，{{ camera_move }}，动作必须物理连贯，衣料和头发随运动自然摆动，道具{{ object_summary }}保持位置合理。对白参考：{{ dialogue }}。
[negative_prompt]
动作跳变，物理不合理，角色漂移，脸部变形，服装颜色改变，发型改变，手部错误，穿模，闪烁，低清晰度，水印，文字乱码
[parameter_suggestion]
可灵：优先选择高表现/高质量模式；动作复杂时用5s，叙事对白镜头用10s；必要时上传首帧或尾帧锁定人物与构图。
[aspect_ratio]
16:9
[duration]
{{ duration }}
